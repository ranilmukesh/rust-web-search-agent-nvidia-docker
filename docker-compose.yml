services:
  rig-ddg-agent:
    build: .
    environment:
      NVIDIA_API_KEY: ${NVIDIA_API_KEY}
      RIG_OPENAI_BASE_URL: ${RIG_OPENAI_BASE_URL:-https://integrate.api.nvidia.com/v1}
      RIG_MODEL: ${RIG_MODEL:-deepseek-ai/deepseek-v4-pro}
    command: ["What is Rig and how do I get started?"]
